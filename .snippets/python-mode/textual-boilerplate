# -*- mode: snippet -*-
# name: textual-boilerplate
# key: app
# expand-env: ((yas-indent-line 'fixed))
# --

from textual.app import App, ComposeResult
from textual.containers import ${1:containers}
from textual.widgets import ${2:widgets}

class ${3:app}(App[None]):

    def compose(self) -> ComposeResult:
        yield $0

if __name__ == "__main__":
    $3().run()
